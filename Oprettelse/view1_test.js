'use strict';

describe('myApp.Oprettelse module', function() {

  beforeEach(module('myApp.Oprettelse'));

  describe('Oprettelse controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});